name: feature_movie
version: 0.0.1

publish_to: none

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_dotenv: 5.2.1

  core:
    path: ../core

flutter:
  assets:
    - .env

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0